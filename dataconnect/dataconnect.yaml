specVersion: "v1"
serviceId: "myapptest"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "myapptest-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
